#include <iostream>
#include <math.h>
using namespace std;
int main()
{
  int z = -10;
  double x = 2.3;

    double y = 0.25;
    cout << "Absolute value of z=-10 : " << abs(z) << endl;
    cout << "Power value: x^y = (2.3^0.25) : " << pow(2, 3) << endl;
    //2*2*2

        x = 4.56;
    cout << "Floor value of x=4.56 is : " << floor(x) << endl;
    cout<<floor(1.8);
    y = 11.9;
    cout << "Ceiling value of y=12.3 : " << ceil(y) << endl;
    y = 0.25;
    cout << "Square root value of y=0.25 : " << sqrt(y) << endl;
}
